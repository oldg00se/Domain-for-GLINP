(define (problem barman)
        (:domain barman)
        (:init
                (and
                (not (holding))
                (empty)
                (not (containIg1))
                (not (containIg2))
                (not (containIg3))
                (not (containCt))
                (> (numg) 0)
                (> (numig1) 0)
                (> (numig2) 0)
                (> (numig3) 0)
                (> (numct) 0)
                (= (numg) (numct))
                (>= (numig1) (numct))
                (>= (numig2) (numct))
                (>= (numig3) (numct))
                )
        )
        (:goal
              (and
              (= (numct) 0)
              (empty)
              (not (holding))
              )
        )
)