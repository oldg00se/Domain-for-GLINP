(define (problem chopTree-pro)
        (:domain chopTree)
        (:init
            (and
              (>= (h) 0)
            )
        )
        (:goal
              (and
                  (= (h)  0)
              )
        )
)

