(define (problem blockclear-pro)
        (:domain blockclear)
        (:init
              (and
                 (empty)
                 (> (n) 0)
              )
        )
        (:goal
              (and
                  (empty)
                  (= (n)  0)
              )
        )
)

