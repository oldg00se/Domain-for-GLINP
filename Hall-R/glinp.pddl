(define (problem pro-Hall-R)
        (:domain Hall-R)
        (:init
            (and
                (not (visitrt))
                (not (visitrb))
                (not (visitlt))
                (not (visitlb))
                (dectb)
                (not (dectl))
                (not (dectt))
                (not (dectr))
                (= (disr) 0)
                (>= (disl) 0)
                (>= (dist) 0)
                (>= (disb) 0)
                (= (disl) (startl))
                (= (dist) (startt))
            )
        )
        (:goal (and
                   (visitrt)
                   (visitlt)
                   (visitlb)
                   (visitrb)
                   (= (disl) (startl))
                   (= (dist) (startt))
               )
        )
)